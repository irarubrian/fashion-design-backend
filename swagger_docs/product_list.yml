summary: Get paginated list of products
description: Fetch paginated products with optional page and per_page query params.
parameters:
  - name: page
    in: query
    type: integer
    default: 1
  - name: per_page
    in: query
    type: integer
    default: 10
responses:
  200:
    description: A list of products